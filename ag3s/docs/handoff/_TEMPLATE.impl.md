# <STEP> — 구현

> writer: ag3s-implementer (A1) · 읽는 쪽: verifier, scribe, lead

## 무엇을 했나 (평이한 요약 먼저)
<두세 문장>

## 바뀐 파일
| 파일:줄 | 무엇이 | 왜 |
|---|---|---|
| `benchmark/...py:123` | | |

## 단위 검증
```bash
<돌린 명령>
```
<결과 — pass/fail 개수>

## verifier 가 알아야 할 것
- 새로 생긴 플래그·기본값 변경: <없으면 "없음">
- 재생산이 필요한 산출물(npz 등): <없으면 "없음">
- 옛 기록과 호환이 깨지는가: <없으면 "없음">

## 내가 기대하는 결과
<**verifier 는 측정이 끝나기 전에 이 절을 읽지 않는다.** 기대가 보이면 판정이 뒤집힌다>
