# AG3S / trajopt 검토·개발용 의존성 (CPU 파이프라인)
#
# 이 목록은 `benchmark/ag3s` 와 `benchmark/trajopt` 를 돌리는 데 실제로 필요한 것만 담는다.
# openpi / jax / VLA 정책 추론은 **포함하지 않는다** — 그쪽은 로컬 PC 의 `pi05_infer.py` 몫이고,
# 이 컨테이너에서는 기록된 롤아웃(run_0004)을 재생해서 쓴다.
#
# curobo 는 여기 없다. numpy/scipy 를 끌어올려 이 환경을 깨뜨릴 수 있어 별도 venv 에 설치한다 —
# `requirements-curobo.txt` 참고.
#
# 버전은 2026-09-11 기준 검증된 조합이다.

numpy==2.4.6
scipy==1.15.3

# MuJoCo — 기록된 롤아웃 재생과 depth/segmentation 렌더링. 헤드리스면 MUJOCO_GL=osmesa
mujoco==3.11.0

# CasADi — `constraint_builder.py` 의 심볼릭 제약, `urdf_sphere_chain.py` 의 FK.
# (씬 primitive 경로는 삭제 예정이지만 로봇 FK/Jacobian 은 계속 쓴다)
casadi==3.8.0

# OSQP — trajopt 의 QP 솔버
osqp==1.1.3

PyYAML==6.0.2

# 그림·리포트용
matplotlib==3.10.3
imageio==2.37.0

pytest==8.3.4
